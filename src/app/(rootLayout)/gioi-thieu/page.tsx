import { Wrapper } from "@/components/shared";

type AboutPageProps = {}

export default function About(props : AboutPageProps) {
    return (
        <Wrapper>
            
        </Wrapper>
    )
}